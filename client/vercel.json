{
  "framework": "vite",
  "buildCommand": "npm run build --workspace @mcp-registry/client",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
